# Stack_Learning
Your code implements a Fixed-Size Stack data structure using an array (a Python list). A stack follows the LIFO (Last-In, First-Out) principle, meaning the last item you add to it is the very first one you take out—just like a physical stack of dinner plates.
